The mendeley/ directory contains the CMake build system for breakpad plus
a set of patches that were applied to the original upstream checkout.

When updating to a newer breakpad version, any relevant patches from patches/
should be re-applied.

